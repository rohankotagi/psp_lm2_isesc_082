#include<stdio.h>
int add(int a, int b)
{
    int sum;
    sum= a+b;
  
    
}


int main(){
    int a, b, sum;
    printf("Enter the 2 numbers to be added");
    scanf("%d %d", &a, &b);
    sum=add(a,b);
    printf("The sum of %d and %d is %d" ,a, b, sum);
    return 0;
}
